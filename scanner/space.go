package scanner

/*
import (
	"github.com/PaulioRandall/scarlet-go/token"
)

// scanSpace returns the next whitespace token in the source. Adjacent
// whitespace symbols are combined into a single whitespace token.
func (s *source) scanSpace() t token.Token {
	start := s.i
	for (ru := range s.rs) {

	}
}
*/
