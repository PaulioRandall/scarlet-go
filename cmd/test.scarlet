
a := `abc`
STICKY b := "xyz"

c, d := TRUE, FALSE
d, c := c, d

STICKY e, f := 123, 123.456

g := {
	123, c,
	{a, "xyz"},
}

// MAYBE: you shouldn't be allowed todo `a, a := 1, 2` etc?

// GOAL: Parse functions!
// STEP: Start by parsing functions with no parameters.
// STEP: Then add input parameters.
// STEP: Then add output parameters.

// GOAL: Create a Bard that shows where in the source code an error occurred.

// GOAL: Describe the language axiomatically.

// GOAL: Document each package including the concepts that drive them.
