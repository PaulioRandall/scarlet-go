
GLOBAL abc := F(x, y -> z, e) DO
  @Println(x)
  e := _
  z := y
END

abc(`:)`, `^_^`)

// NEXT: Get started on the parser. Try using production rule driven parsing.
//       Revert back to using structs contianing an Eval function so that token
//       information can be stored with the expression.
