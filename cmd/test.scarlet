
GLOBAL abc := F(x, y -> z, e) DO
  @Println(x)
  e := _
  z := y
END

abc(`:)`, `^_^`)

// NEXT: Update grammer and scanner to include all remaining token types.
// THEN: Merge strimmer into evaluator.
// THEN: Update evaluator to strip ends from string templates.
// THEN: Add 1-token buffer struct to lexor.
